---
description: Use when the user wants to set up their studio vocabulary — what they call programmes, classes, sessions, trainers. Also use when the user says "you're using the wrong words" or this is their first session with Zooza MCP.
---

Call `get_skill` with `name: "negotiate-terminology"` to load the vocabulary interview playbook, then follow it step by step.
