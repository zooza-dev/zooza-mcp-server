---
description: Use when the user wants to create a new class, group, or course in Zooza — e.g. "set up a Monday yoga class", "add a new group", "schedule a course". Guides through programme selection, venue, trainer, session pattern, and commits.
---

Call `get_skill` with `name: "class-management"` to load the full guided playbook, then follow it step by step.
